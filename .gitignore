.DS_Store
DerivedData/
build/
*.xcodeproj
xcuserdata/
# Gerado pelo XcodeGen a partir de project.yml (info.properties) — project.yml é a fonte de verdade
App/Info.plist
.superpowers/brainstorm/

# Documentação de trabalho é local. A memória padrão versionada é CLAUDE.md.
/docs/

# Segredos de produção — só existem no VPS, nunca no repo
infra/.env

# Storage do modo dev (memory storage persistido em disco) — só local
server/.dev-storage/
